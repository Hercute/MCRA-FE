const LoginPage = () => {
  return (
    <div>
      <form action="">
        <div>logo</div>
        <input type="text" />
        <button>계속</button>
      </form>
      <button>구글로 로그인</button>
      <button>회원가입</button>
    </div>
  );
};

export default LoginPage;
